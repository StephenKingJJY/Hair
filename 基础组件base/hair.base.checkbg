;框架内部程序用来检测框架是否运行
;命令版本号v1.2

to hair.base.checkbg
localmake "timeauth hair.base.timeauth
if name? "hair_preauth [op "true]
if not name? "hair_timeauth [op "false]
ifelse and (abs :timeauth - :hair_timeauth) > 65 (abs :timeauth - :hair_timeauth + 86400) > 65 [op "false][op "true]
end
bury "hair.base.checkbg
