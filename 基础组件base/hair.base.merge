;合并元素
;字+字=字，表+表=表，字+表=[字 表]
;数字作为字来判断
;代码版本v1.2

to hair.base.merge :v1 :v2
hair.detect
if and word? :v1 word? :v2 [op word :v1 :v2]
if and list? :v1 list? :v2 [op se :v1 :v2]
op list :v1 :v2
end
bury "hair.base.merge
